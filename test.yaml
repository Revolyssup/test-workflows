name: User
services:
  istiodeploy:
    type: $(#use https://raw.githubusercontent.com/Revolyssup/test-workflows/main/pattern/lol.yaml)
    namespace: default
    settings:
      controlplaneNS: istio-system
      sidecarNS: default
      istioVersion: 1.12.0